version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:3001/api}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    env_file:
      - path: .env
        required: false
